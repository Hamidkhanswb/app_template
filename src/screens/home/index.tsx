import {ScreenWrapper} from '@components';
import React from 'react';
import {Text} from 'react-native';

const Home = () => {
  
  return (
    <ScreenWrapper>
      <Text>Home</Text>;
    </ScreenWrapper>
  );
};

export default Home;
